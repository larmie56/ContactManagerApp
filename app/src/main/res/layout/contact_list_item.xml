<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/image_text_drawable"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:background="@color/deep_purple_900"/>

    <TextView
        android:id="@+id/text_contact_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textSize="17sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="@id/image_text_drawable"
        app:layout_constraintStart_toEndOf="@id/image_text_drawable"
        app:layout_constraintBottom_toBottomOf="@id/image_text_drawable"
        tools:text="John Doe"/>

    <ImageView
        android:id="@+id/image_phone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_phone"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toStartOf="@id/image_text"
        app:layout_constraintTop_toTopOf="@id/text_contact_name"
        app:layout_constraintBottom_toBottomOf="@id/text_contact_name"
        app:tint="@color/lime_A400" />

    <ImageView
        android:id="@+id/image_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_text"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/text_contact_name"
        app:layout_constraintBottom_toBottomOf="@id/text_contact_name"
        app:tint="@color/deep_purple_900" />


</androidx.constraintlayout.widget.ConstraintLayout>